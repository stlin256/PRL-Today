[project]
name = "PRL-Today"
version = "0.1.0"
description = "Always-on-top PRL daily mining profit floating window."
requires-python = ">=3.10"
dependencies = ["PyQt5>=5.15"]

[project.scripts]
prl-today = "prl_profit_float.app:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
